package contact.management;

import java.io.Serializable;
import java.util.ArrayList;

public class CarteDeTelefon extends ArrayList
        implements Serializable {


//    private enum FilterOption {
//            
//    DUPA_NUME,
//    DUPA_PRENUME,
//    DATA_NASTERII,
//    DUPA_NUMAR_TELEFON;
//    }
//    private List<Contact> findPerson(String searchString, FilterOption filter) {
//        switch (filter) {
//            case DUPA_NUME: return oameni.stream().filter(person -> person.getPrenume().equals(searchString)).collect(Collectors.toList());
//            case DUPA_PRENUME: return oameni.stream().filter(person -> person.getNume().equals(searchString)).collect(Collectors.toList());
//            default:
//                System.out.println("Invalid filter option");
//                return new ArrayList<>();
//        }
//    }
}
